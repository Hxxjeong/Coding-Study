# [다이얼](https://www.acmicpc.net/problem/5622)

<br>

## 시간복잡도 분석
-  O(L)

## 코드
```java
package baekjoon.bronze;

import java.io.*;

public class b251014_5622_다이얼 {
	public static void main(String[] args) throws IOException{
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		String word = br.readLine();
		int time = 0;
		
		// 영단어를 숫자로 바꾸기
		// ABC는 3초 DEF는 4초 ...
		for(int i=0;i<word.length();i++) {
			char c = word.charAt(i);
			
			switch(c) {
				case 'A': case 'B': case 'C':
					time+=3;break;
				case 'D': case 'E': case 'F':
					time+=4;break;
				case 'G': case 'H': case 'I':
					time+=5;break;
				case 'J': case 'K': case 'L':
					time+=6;break;
				case 'M': case 'N': case 'O':
					time+=7;break;
				case 'P': case 'Q': case 'R': case 'S':
					time+=8;break;
				case 'T': case 'U': case 'V':
					time+=9;break;
				case 'W': case 'X': case 'Y': case 'Z':
					time+=10;break;
			}
		}
		
		System.out.println(time);
		
	}
}




/*
package baekjoon.bronze;

import java.io.*;

public class b251014_5622_다이얼 {
	public static void main(String[] args) throws IOException{
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		String word = br.readLine();
		int time = 0;
		
		// 영단어를 숫자로 바꾸기
		// ABC는 3초 DEF는 4초 ...
		for(int i=0;i<word.length();i++) {
			char c = word.charAt(i);
			
			switch(c) {
				case 'A': case 'B': case 'C':
					time+=3;break;
				case 'D': case 'E': case 'F':
					time+=4;break;
				case 'G': case 'H': case 'I':
					time+=5;break;
				case 'J': case 'K': case 'L':
					time+=6;break;
				case 'M': case 'N': case 'O':
					time+=7;break;
				case 'P': case 'Q': case 'R': case 'S':
					time+=8;break;
				case 'T': case 'U': case 'V':
					time+=9;break;
				case 'W': case 'X': case 'Y': case 'Z':
					time+=10;break;
			}
		}
		
		System.out.println(time);
		
	}
}




/*
import java.io.*;

public class b251014_5622_다이얼 {
    public static void main(String[] args) throws IOException {
        // 빠른 입력 받기
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String word = br.readLine(); // 입력 문자열 (대문자만)

        // 알파벳 A~Z에 해당하는 다이얼 시간 저장용 배열
        int[] dial = new int[26]; // 0번 인덱스 = 'A', 25번 인덱스 = 'Z'

        // 알파벳에 따라 다이얼 시간 설정
        // ABC -> 3초
        dial['A' - 'A'] = 3;
        dial['B' - 'A'] = 3;
        dial['C' - 'A'] = 3;

        // DEF -> 4초
        dial['D' - 'A'] = 4;
        dial['E' - 'A'] = 4;
        dial['F' - 'A'] = 4;

        // GHI -> 5초
        dial['G' - 'A'] = 5;
        dial['H' - 'A'] = 5;
        dial['I' - 'A'] = 5;

        // JKL -> 6초
        dial['J' - 'A'] = 6;
        dial['K' - 'A'] = 6;
        dial['L' - 'A'] = 6;

        // MNO -> 7초
        dial['M' - 'A'] = 7;
        dial['N' - 'A'] = 7;
        dial['O' - 'A'] = 7;

        // PQRS -> 8초
        dial['P' - 'A'] = 8;
        dial['Q' - 'A'] = 8;
        dial['R' - 'A'] = 8;
        dial['S' - 'A'] = 8;

        // TUV -> 9초
        dial['T' - 'A'] = 9;
        dial['U' - 'A'] = 9;
        dial['V' - 'A'] = 9;

        // WXYZ -> 10초
        dial['W' - 'A'] = 10;
        dial['X' - 'A'] = 10;
        dial['Y' - 'A'] = 10;
        dial['Z' - 'A'] = 10;

        int time = 0;

        // 입력된 문자열을 한 글자씩 처리
        for (int i = 0; i < word.length(); i++) {
            char c = word.charAt(i); // 현재 문자
            int index = c - 'A';     // 배열 인덱스로 변환
            time += dial[index];     // 해당 문자에 대한 시간 더하기
        }

        // 최종 결과 출력
        System.out.println(time);
    }
}
*/

```